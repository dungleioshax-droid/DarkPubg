name: Xcode - Build and Analyze

# Tạm chỉ chạy tay (workflow_dispatch) để dành macOS minutes cho Build Release.
# Bật lại push khi quota rủng rỉnh.
on:
  workflow_dispatch:
  pull_request:
    branches: [ "main" ]

env:
  XCODE_VERSION: '15.4'

jobs:
  build:
    name: Build and analyse using xcodebuild command
    runs-on: macos-14

    steps:
      - name: Setup Xcode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: |
          xcodebuild clean build analyze \
            -scheme DarkSpeed.Sim \
            -project DarkSpeed.xcodeproj \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            ARCHS=arm64 \
            ONLY_ACTIVE_ARCH=YES \
            | xcpretty && exit ${PIPESTATUS[0]}
